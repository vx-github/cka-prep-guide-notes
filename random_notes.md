# Random k8s notes

```
'k create RESOURCE' is een mooie standaard manier om resources aan te maken. Maar 'k create deploy' heeft helaas veel minder opties dan de oude 'k run'.
11:34
Die opties helpen bijvoorbeeld enorm als je een yaml wilt genereren met '--dry-run -o yaml'
```